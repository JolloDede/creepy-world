import { Structure } from "../chars/structure";

/**
 * DragManager manages the Element your holding in your hand a the time
 */
export class DragManager {
    private structure: Structure;

    constructor() {

    }

    holdStructure() {
        this.structure = 
    }
}

export enum Structures {
    Collector,
    Blaster,
    
}